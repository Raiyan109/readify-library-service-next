import React from 'react'

export const FilterResult = ({ name, cover,
    desc, author, genre, pages,
    isRented,
    stock, rentPrice, sellPrice, sold }) => {
    return (
        <div>{name}</div>
    )
}
